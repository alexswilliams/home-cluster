# Hard limits across the namespace.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-resource-quota
  namespace: default
spec:
  hard:
    requests.memory: 256Mi
    requests.cpu: 200m
    limits.memory: 256Mi
    limits.cpu: 200m
