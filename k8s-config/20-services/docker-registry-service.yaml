apiVersion: v1
kind: Service
metadata:
  name: docker-registry-service
  labels:
    app: docker-registry
spec:
  type: ClusterIP
  ports:
  - port: 5000
    name: registry-port-external
    targetPort: registry-port
  selector:
    app: docker-registry
